using FileParser_Samples_Tests.Models;
using Sprache;
using System;
using System.Collections.Generic;
using System.Text;

namespace FileParser_Samples_Tests
{
    public class ICPGrammar
    {
        //public static readonly Parser<ParsedData> Export = from rows in BaseCsvGrammar.DataRows
                                                            //select new ParsedData(rows.Skip(1), rows.Take(1));
    }
}
